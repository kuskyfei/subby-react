const images = require('./images')
const manifest = require('./manifest')
const settingsMessage = require('./settingsMessage')
const settings = require('./settings')

module.exports = {images, manifest, settingsMessage, settings}
